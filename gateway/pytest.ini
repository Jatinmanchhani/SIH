[pytest]
# Modules under gateway/ import each other with bare names (e.g. `from router import route`),
# so tests need gateway/ itself on sys.path — pytest does this automatically as long as
# tests run from here and there's no conflicting package __init__.py at this level.
testpaths = tests